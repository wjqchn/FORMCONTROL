# 质量文控平台 — 项目汇总（封板文档）

> 封板日期：2026-08-14
> 项目目录：`C:\Users\王嘉琦\Desktop\表格整理`
> 远程仓库：`https://github.com/wjqchn/FORMCONTROL.git`（注意无横杠）
> 安装路径：`C:\Users\Public\FORMCONTROL`

---

## 一、项目概述

「质量文控平台」是一套面向质量文控场景的 Windows 桌面应用，用于管理 **FG（表格）** 与 **SG（文件）** 两大类记录的录入、分类汇总、版本追踪、权限管理、Excel 批量导入导出与自动数据库备份。

项目包含两个可独立交付的安装包版本：

- **1.0**：无账户体系的文控版本。
- **2.0**：在 1.0 基础上增加账户、角色、权限与首次管理员初始化，为正式版本。

---

## 二、版本与交付物

| 版本 | 安装包 | 大小 | 状态 | 适用范围 |
|---|---|---|---|---|
| 1.0 | `1.0/dist_pkg/FormControl1.0_Setup.exe` | 约 15.11 MiB | 已封板保留 | 无账户体系的表格/文件分类汇总与备份 |
| 2.0 | `2.0/dist_pkg/FormControl2.0_Setup.exe` | 约 15.46 MiB | 已封板保留 | 带账户、角色、权限和首次管理员初始化的正式版本 |

安装包均采用 **PyInstaller + Inno Setup 6** 流程，安装后在 `C:\Users\Public\FORMCONTROL` 运行本地服务，桌面快捷方式名为「质量文控平台」。

---

## 三、技术架构

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | 原生 HTML/CSS/JavaScript（单文件，无框架） | 主页面 `表格分类汇总.html`、登录页 `login.html`、管理端 `admin.html` |
| 后端 | Python 标准库 `http.server` + `sqlite3` | `server/server.py`，仅监听 `127.0.0.1` |
| 数据库 | SQLite | `data.sqlite`（业务数据）；`auth.sqlite`（账户/角色/会话，仅 2.0） |
| 打包 | PyInstaller → Inno Setup 6 | exe 托管本地服务，安装包释放 exe + HTML + 图标到安装目录 |
| 启动 | 桌面快捷方式 → `FormControl.exe` | exe 启动本地 HTTP 服务并自动打开系统浏览器 |

架构演进：初期为纯 HTML + 浏览器 IndexedDB（数据隔离在浏览器）；当前为捆绑本地 Python 服务 + SQLite（数据落盘到安装目录，不依赖浏览器存储）。

---

## 四、1.0 功能总结

1. **录入与编辑**：FG/SG 字段完整，录入区三列网格布局，编辑弹窗复用同一表单面板（严格等宽两列）。
2. **分类汇总展示**：按「大类 → 亚类」树状分组；同一编号下最新版本在上、历史版本在下；仅旧版本显示红色「历史」标签；编号分色（FG 蓝、SG 绿）；未配置类名时显示笔形编辑按钮就地命名。
3. **培训状态管理**：SG 行培训列使用 iOS 风格胶囊开关（已培训/待培训）。
4. **Excel 批量导入导出**：导入支持 FG/SG 混合，自动检测重复并区分新增/覆盖/跳过；新增大类/亚类时弹出完整树状确认框（新增节点红色高亮 +「将新增」标签，既有节点灰色「已有」标签）；导入完成后立即刷新汇总。
5. **自动数据库备份**：
   - 快照默认路径 `D:\文档管理数据库备份\YYYYMMDD_HHMMSS\`，含 `data.sqlite` 与 `backup.json`。
   - 触发时机（可独立开关）：编辑前、删除前、导入前、恢复前；明确不触发：新增、打开编辑弹窗、未保存修改。
   - 备份查询按日期树状分组；过期清理按阈值（默认 1 GB）计算占用百分比，<75% 绿、75%–98% 黄红、≥99% 深红；清理时移入 Windows 回收站，非永久删除。
6. **数据库恢复**：页面内确认弹窗（非原生 confirm），恢复前自动创建快照。

---

## 五、2.0 功能总结（在 1.0 基础上）

2.0 的核心增量是**账户安全与系统管理能力**：

1. **认证与会话**：登录/登出，基于 `Authorization: Bearer <token>` 的会话控制（token 64 字符 hex，8 小时超时，活跃自动续期）；密码哈希 PBKDF2-SHA256（100000 轮 + 随机 salt）。
2. **用户/角色/权限管理**：权限覆盖录入、编辑、导入、导出、备份与管理端访问；管理员角色自动拥有全部权限 + 管理端访问权。
3. **账户不物理删除**：失活账户禁用登录与编辑，可通过「激活」恢复；系统账号受保护。
4. **首次管理员初始化**：全新部署时登录页直接显示「请设置管理员密码」，固定展示默认管理员用户名 `admin`，仅需「设置新密码」+「确认密码」；**不展示或要求输入预置密码/授权码**；中途关闭网页不残留会话，下次仍回到设置引导。
5. **密码复杂度规则**：至少 8 位，且大写字母、小写字母、数字、特殊符号（`!@#$%^&*()_+-=[]{}|;:,.~`）四类中至少包含三类；中、低强度禁止提交，高/安全强度可提交；后端同步校验。
6. **普通账户强制改密**：管理员新建或重置的账户带 `must_change_pw=1`，首次登录后强制修改密码方可进入主页面。
7. **账户备份**：账户数据库独立保存并支持账户备份。
8. **UI 风格**：白色工作台、浅色渐变 Hero、紫蓝主操作、绿色状态、红色危险操作；管理端为浅色管理界面。

---

## 六、关键 API 接口

### 1.0（业务与备份）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/records` | 获取全部记录 |
| POST | `/api/records` | 覆盖写入全部记录 |
| GET | `/api/settings` | 读取设置 |
| POST | `/api/settings` | 保存设置 |
| GET | `/api/backups/status` | 备份状态（最新时间、过期占用） |
| GET | `/api/backups` | 备份列表 |
| POST | `/api/backups/settings` | 保存备份设置 |
| POST | `/api/backups/restore` | 恢复指定快照 |
| POST | `/api/backups/cleanup` | 清理过期备份 |
| POST | `/api/quit` | 关闭服务 |
| GET | `/api/health` | 健康检查 |

### 2.0（新增认证与管理）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/api/auth/login` | 登录 | 公开 |
| GET | `/api/auth/session` | 获取当前用户 | 需登录 |
| POST | `/api/auth/logout` | 登出 | 需登录 |
| GET | `/api/auth/bootstrap` | 查询是否需要首次设置默认管理员密码（不返回密码或令牌） | 公开 |
| POST | `/api/auth/setup-admin-password` | 首次设置默认管理员密码（成功后创建正式会话） | 公开，仅首次部署 |
| POST | `/api/auth/change-password` | 修改自身密码（成功后清除强制改密标记） | 需登录 |
| GET | `/api/admin/users` | 用户列表 | 管理员 |
| POST | `/api/admin/users` | 创建/编辑用户 | 管理员 |
| POST | `/api/admin/users/deactivate` | 失活用户（账户不删除） | 管理员 |
| POST | `/api/admin/users/activate` | 重新激活失活用户 | 管理员 |
| GET | `/api/admin/roles` | 角色列表 | 管理员 |
| POST | `/api/admin/roles` | 创建/编辑角色 | 管理员 |
| POST | `/api/admin/roles/delete` | 删除角色 | 管理员 |

---

## 七、构建与发布流程

```
server/server.py
    ↓ PyInstaller (--onefile --windowed --name FormControl)
build/dist_release/FormControl.exe
    ↓ Inno Setup (build/FormControl.iss)
dist_pkg/FormControl_Setup.exe
```

**安装行为**：
- 释放到 `C:\Users\Public\FORMCONTROL`。
- 当前用户桌面创建快捷方式「质量文控平台」。
- 安装前自动终止旧进程（`taskkill` + Mutex 检测）。
- 卸载前优雅关闭服务（`--shutdown` → `/api/quit`）；卸载后 `data.sqlite` / `auth.sqlite` 保留。
- 随包文件：`FormControl.exe`、`login.html`、`admin.html`、`表格分类汇总.html`、`图标.ico`。

**2.0 已验证项**：安装包文件头有效；健康检查 `/api/health` 返回 200；登录页无明文预置密码；首次初始化流程与密码强度校验通过（中强度被服务端拒绝、高强度可完成初始化）；空 `auth.sqlite` 自动补建默认角色与账户；测试实例已关闭、端口已释放。

---

## 八、封板文件清单（保留项）

### 1.0 保留
- `1.0/dist_pkg/FormControl1.0_Setup.exe`（最终安装包）
- `1.0/表格分类汇总.html`、`1.0/server/`、`1.0/FormControl.spec`、`1.0/图标.ico`
- `1.0/build/FormControl.iss`、`1.0/build/FormControl.spec`（构建脚本）
- `1.0/项目总结.md`、`1.0/自动数据库备份功能概览.md`、`1.0/overview.md`、`1.0/README.md`

### 2.0 保留
- `2.0/dist_pkg/FormControl2.0_Setup.exe`（最终安装包）
- `2.0/表格分类汇总.html`、`2.0/login.html`、`2.0/admin.html`、`2.0/server/server.py`
- `2.0/build/FormControl.iss`、`2.0/build/FormControl.spec`、`2.0/build/make_lnk.py`、`2.0/build/质量文控平台.spec`、`2.0/build/dist_release/FormControl.exe`
- `2.0/data.sqlite`、`2.0/auth.sqlite`、`2.0/auth_backups/`、`2.0/backup_settings.json`、`2.0/图标.ico`
- `2.0/overview.md`、`2.0/packaging_overview.md`

### 默认保留的运行数据
- `2.0/data.sqlite`、`2.0/auth.sqlite`、`2.0/auth_backups/`、`2.0/backup_settings.json`（可能含开发过程业务记录与账号信息，不清理）。

---

## 九、封板清理记录

按确认的 **A + B 类** 清理方案执行：

- **A 类（构建缓存、编译临时文件、空日志）已清理**：`1.0/build/` 下 14 个 PyInstaller 构建输出目录（dist_*/work_*/spec_*/FormControl）、`2.0/build/` 下 `pyi_work/`、`质量文控平台/`、`dist_release/`；`1.0/dist_pkg/` 与 `2.0/dist_pkg/` 下共 16 个 `RCX*.tmp`；临时文件 `2.0/.tmp_*`、`2.0/server.log`、`*/server/__pycache__`。保留全部 `.iss`/`.spec` 构建脚本与 `dist_release/FormControl.exe`。
- **B 类（阶段性预览/展示文件）按用户要求不保留**：含旧版 UI 展示与效果图预览 HTML/PNG，均不影响正式安装包与源代码。

所有删除均采用 Windows 回收站方式分批执行并逐项核验。

---

## 十、已知事项与后续

1. **Git 推送**：本地已提交，远程推送因网络问题（`curl 55 Connection was reset`）未完成，需在宿主机本机执行 `git push -u origin master`。
2. **冗余备份目录**：封板过程中曾生成的安全备份目录 `封版清理备份_20260814/` 已清除（其内容与活动目录重复，预览文件按用户要求亦不需保留），项目目录现已整理干净。
3. **展示用预览文件**：用户已确认展示/预览类文件无需保留，故未列入交付物。
